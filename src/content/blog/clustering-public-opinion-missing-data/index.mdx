---
title: Clustering public opinion, and how I learned to stop worrying about missing data
standfirst: A standfirst line that says what the reader is about to get, so nobody has to gamble nine minutes on a title alone.
summary: Multiple imputation with mice, then k-means on top. What survived the sensitivity checks, and what quietly did not.
date: 2026-07-14
topic: R
---

Body copy sits at 15.5px on a 620px measure — a little under 80 characters a
line, which is where long-form actually wants to be. The first paragraph gets
no indent; subsequent ones are separated by space rather than indentation.

Inline code like `mice::complete()` and inline
[links](https://www.data-to-viz.com/) stay in the beige-and-accent palette.
No blue anywhere.

## A section heading

Headings use Plus Jakarta Sans at 23px, weight 600, so they read as structure
without shouting. Two levels are enough for a blog — H2 for sections, H3 if a
section genuinely needs sub-parts.

> A pull quote or the one sentence you want people to screenshot, marked only
> by a thin accent rule.

```r
imputed <- mice(survey, m = 20, method = "pmm")
fit     <- with(imputed, kmeans(scale(vars), 4))
```

### A sub-part, for completeness

A list, because migrated posts have plenty of them:

- Multiple imputation with `mice`, twenty imputed datasets
- k-means on the scaled variables, four clusters
- Sensitivity checks on both

And a small table:

| Cluster | Share | Stability |
| ------- | ----- | --------- |
| 1       | 31%   | high      |
| 2       | 24%   | high      |
| 3       | 27%   | medium    |
| 4       | 18%   | low       |

A closing paragraph, ideally with the thing that did not work admitted out
loud.
